[DEFAULT]
# Look for a tag matching the upstream version when creating a tarball
# Only used if no pristine-tar is available
upstream-tree = tag
# Tag format
upstream-tag = release-%(version)s
debian-tag = debian/%(version)s
# Per distribution branch
debian-branch = pkg/debian/master
